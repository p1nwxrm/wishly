class AppConstants {
  // Base URL for the FastAPI backend (10.0.2.2 is for Android emulator)
  static const String baseUrl = 'http://10.0.2.2:8000';

  // Timeout duration for network requests
  static const int connectionTimeoutSeconds = 10;
}